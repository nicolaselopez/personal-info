import { styled } from 'nativewind';
import { View as BaseView, Text as BaseText } from 'react-native';
import { TextInput as BaseTextInput, Button as BaseButton } from 'react-native-paper';

export const View = styled(BaseView);
export const Text = styled(BaseText);
export const TextInput = styled(BaseTextInput);
export const Button = styled(BaseButton);
